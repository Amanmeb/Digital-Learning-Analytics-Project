FROM python:3.12-slim

WORKDIR /app

COPY edge/requirements_receiver.txt .
RUN pip install --no-cache-dir -r requirements_receiver.txt

COPY . .

EXPOSE 8000

CMD ["uvicorn", "edge.device_receiver:app", "--host", "0.0.0.0", "--port", "8000"]